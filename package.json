{
  "name": "bbtodo",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "server",
    "web"
  ],
  "scripts": {
    "build": "npm run build --workspace @bbtodo/server && npm run build --workspace @bbtodo/web",
    "dev": "concurrently \"npm run dev --workspace @bbtodo/server\" \"npm run dev --workspace @bbtodo/web\"",
    "test": "npm run test --workspace @bbtodo/server && npm run test --workspace @bbtodo/web"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "typescript": "^5.9.3"
  }
}
