{
  "name": "bbtodo",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/api",
    "apps/web"
  ],
  "scripts": {
    "build": "npm run build --workspace @bbtodo/api && npm run build --workspace @bbtodo/web",
    "dev": "concurrently \"npm run dev --workspace @bbtodo/api\" \"npm run dev --workspace @bbtodo/web\"",
    "test": "npm run test --workspace @bbtodo/api && npm run test --workspace @bbtodo/web"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "typescript": "^5.9.3"
  }
}
